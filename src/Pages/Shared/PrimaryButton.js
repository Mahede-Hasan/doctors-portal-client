import React from 'react';

const PrimaryButton = () => {
    return (
        <div>
            <button className="btn btn-secondary bg-gradient-to-r from-primary to-secondary text-white bold uppercase  px-6 text-lg">Get Started</button>
        </div>
    );
};

export default PrimaryButton;